# DeepSeek API Key（用于 Hermes 聊天和 RAG embedding 备选）
DEEPSEEK_API_KEY=sk-your-key-here

# Embedding API（备选，本地 fastembed 不可用时使用）
EMBEDDING_API_KEY=
EMBEDDING_API_URL=https://api.deepseek.com/v1/embeddings
EMBEDDING_MODEL=deepseek-chat

# HuggingFace 镜像（加速模型下载）
HF_ENDPOINT=https://hf-mirror.com

# 代理（本机已失效，保留为空）
# HTTP_PROXY=
# HTTPS_PROXY=
